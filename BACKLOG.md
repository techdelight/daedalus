# Backlog

| # | Item |
|---|------|
| 54 | Scaffold conformant project docs — `daedalus docs scaffold [dir]` writes the required-doc skeletons (VISION/ROADMAP/SPRINTS/BACKLOG + README/ARCHITECTURE/CONTRIBUTING/CHANGELOG) from the docs/PROJECT-INIT.md templates, already conformant to the structured-docs contract so `daedalus docs lint` passes on a fresh project. Sibling to `daedalus docs lint`; turns the copy-paste bootstrap into one command and removes the "read docs/PROJECT-INIT.md and hope" step for a new session. See docs/PROJECT-INIT.md for the templates and contract |
| 6 | Shell toggle — switch between Claude Code and a regular project shell inside the container |
| 8 | Bundle release assets — package runtime files into a single tarball on the GitHub Release page instead of individual files |
| 9 | Side-by-side versions — install a new version alongside the existing one, allowing rollback or A/B comparison before switching |
| 11 | Homebrew installation (`brew install daedalus`) — add Homebrew tap, formula generator, and CI automation. See [docs/homebrew-plan.md](docs/homebrew-plan.md) for full plan |
| 16 | ACP integration — use the Agent Client Protocol to communicate with the Claude Code CLI, enabling Daedalus to observe agent state (thinking, tool use, idle, error) in real time |
| 21 | Shared Maven `.m2` repository — mount a host-side `.m2/repository` into containers so dependencies are shared across projects. Investigate overlay/merge strategy: a stable global repo (read-only base) combined with a per-container local repo for builds/downloads/installs, so containers benefit from cached artifacts without polluting the shared cache |
| 25 | Webdev container — move Node.js out of the regular `dev` stage into a dedicated `webdev` build target for web/frontend projects. Keeps the default dev image lean |
| 27 | Decouple tooling from agent runner images — keep base agent containers minimal and let the agent install additional tools at runtime. Provide container snapshotting so customized environments persist across restarts |
| 29 | Mobile WebSocket stability — investigate and fix regular disconnects on mobile web clients (possible causes: browser background tab throttling, network switches between Wi-Fi and cellular, WebSocket ping/pong timeout tuning, reconnect logic) |
| 31 | Rename skill store — rename "skill catalog" / "skill" terminology to avoid confusion with Claude Code's built-in skill repository. Candidate: "focus" (focus catalog, focus files). Open to alternatives |
| 33 | tmux control mode integration — use `tmux -C` control mode instead of raw PTY for terminal interaction. Enables native scrollback access, clean session disconnect/reconnect, and machine-parseable event notifications for agent observability |
| 37 | Shared Claude versions volume — Claude CLI stores its versions in `~/.local/share/claude/versions` inside each container, consuming significant disk space per project. Create a shared Docker volume for this path and mount it into all containers that use a Claude runner |
| 38 | Web UI hangs on trust prompt — when attaching to a container/tmux session where Claude CLI is showing the "trust this folder" security prompt, the Web UI hangs instead of rendering the prompt interactively |
| 39 | Add Maven to dev container — the `dev` build target does not include `mvn`. Install Maven via SDKMAN! in the Dockerfile so Java/Maven projects work out of the box |
| 40 | Fix PATH on container startup — Claude Code reports PATH issues inside the container on startup. Investigate entrypoint.sh and shell profile sourcing |
| 41 | Auto-build missing Docker image — when the Docker image is missing, automatically trigger a build instead of failing. Detect missing image in the launch flow and run `build.sh` |
| 42 | Fix Java in dev container — Java isn't working in the dev build target. Investigate SDKMAN!/JDK installation, JAVA_HOME, and PATH in the Dockerfile dev stage |
| 43 | Multi-repo submodule support — support projects consisting of several services in separate repos via git submodules, so they can be managed as a single Daedalus project with shared skills and configuration |
| 44 | Local LLM support (Ollama) — add a local runner that connects Claude Code to Ollama for companies that cannot use remote LLMs. Investigate Claude Code's Ollama integration and create a `local` runner profile |
| 45 | Post-install onboarding — after download and install, users don't know what to do next. Add a first-run wizard, `daedalus init` command, or getting-started guide that appears after installation |
| 46 | Clarify value proposition — improve README, landing page, and first-run messaging to clearly explain what Daedalus does and why users need it. Focus on the "hands-off AI coding in a safe container" pitch |
| 47 | Coordinator staleness visibility — `daedalus coordinator status` should print the running daemon's build/version and warn when the on-disk `daedalus-coordinator` binary is newer than the running PID. The daemon is long-lived and `EnsureRunning` reuses it, so after `./build.sh` an old daemon keeps running old code with no signal — a real debugging footgun |
| 48 | Coordinator auto-restart on binary change — `EnsureRunning` compares the running daemon's version against the on-disk binary and re-spawns if they differ, so a rebuild is picked up automatically instead of requiring a manual `daedalus coordinator stop`. Builds on #47 |
| 51 | Dockerfile layer efficiency — a new Daedalus version should not re-download Claude, Copilot, or the language toolchains. The `skill-catalog-mcp` / `project-mgmt-mcp` / `daedalus-runner` COPYs sit at the bottom of the `base` stage, upstream of every heavy download in `utils`/`dev`/`godot`/`copilot-*`; `build.sh` rewrites those binaries on every build, so each version bump busts the cache for everything downstream — the Go tarball, SDKMAN's JDK/Maven/Kotlin, the Godot zip, and the Copilot CLI all re-download. Restructure so Daedalus-owned artefacts land last (thin overlay stage, or COPY at the end of each leaf stage) leaving base + toolchain layers stable. Also: version-pin the Claude and Copilot installers (currently unpinned curl-into-bash, so any cache bust silently pulls a different agent version — see the supply-chain warning at the Claude install) and consider apt/SDKMAN cache mounts. Related: #37 (shared Claude versions volume), #27 (decouple tooling from runner images), #25 (webdev stage) |
| 52 | Derive project state from files instead of agent self-reporting — `project-mgmt-mcp`'s three write tools (`report_progress`, `set_vision`, `set_version`) ask the agent to report what already exists on disk, and agent self-reporting is too unreliable to trust. Each has a host-side source: vision ← `VISION.md`, version ← the `VERSION` file (`core.ReadVersion()` already parses it), progress % ← the current sprint's item statuses (`core.ParseSprints` already exposes `Sprint.Items[].Status`, so "3 of 5 Done" = 60%). Have the Daedalus instance derive all three host-side and deprecate the write tools, leaving `project-mgmt-mcp` read-mostly. Derived progress is a proxy (sprint-item completion, not true progress) but it is reliable and explainable, and it drops a trust dependency on the container. Read-on-request is enough to start — `handleDashboard` already reads progress.json per request; an fsnotify watcher is only needed if live push over the WebSocket is wanted later |
| 53 | Per-project required-doc opt-out — the doc checklist (#51's sibling, `core.RequiredDocs()`) is a hardcoded set of 8; some projects legitimately don't want one (e.g. CONTRIBUTING). Add a `DisabledDocs []string` field to the registry's `ProjectEntry` with a v3→v4 migration (nil default, same shape as the v2→v3 field additions in Sprint 23) so the badge denominator adapts — 6/7 rather than 6/8. Open question: how to set it — `daedalus config --set` (which already handles `target=`) or click-to-dismiss in the docs sidebar, which is nicer UX but needs a write endpoint. Also fold in the SPRINTS.md/ROADMAP.md wrinkle: `readSprints` falls back to ROADMAP.md for projects predating the doc split, so a pre-split project is flagged as missing SPRINTS.md while being legitimately healthy |
| 55 | Coordinator start path is missing the extra bind mounts (likely a bug on the now-default runner path) — `internal/coordinator/coordinator.go` builds its `docker compose run` args as only `--detach`, `-e` env, `--name`, `claude`, and never calls `core.BuildExtraArgs` (only the two legacy paths do: `cmd/daedalus/launch.go:73`, `internal/docker/docker.go:112`). `docker-compose.yml` mounts only `${PROJECT_DIR}:/workspace` and `${CACHE_DIR}:/home/claude`, so the runner path lacks the shared skill catalog (`SkillsDir()`→`/opt/skills`), the project-mgmt progress dir (`${PROJECT_DIR}/.daedalus`→`/workspace/.daedalus`), and DinD/display mounts that the legacy path adds. Verify the user-facing impact (skills likely unavailable, MCP progress reporting likely broken on the default path) and restore the needed mounts in the coordinator command. Prerequisite for Milestone 5's shared-volume work (#37/#21/#27), whose mounts must also land here — see docs/milestone-5-plan.md |
| 57 | Control-plane service + Task/Job/Artifact model — a host-side `daedalus-control` (`internal/control`) that owns the authoritative *desired* orchestration state (Tasks = what to do, Jobs = one attempt, Artifacts = a committed result + status) in a Daedalus-owned SQLite store, **reconciled** against reality (not merely stored). The Job (not the session) is the unit of orchestration; Git-native (a managed project must be a Git repo). Milestone 13 (V1). See docs/guild-master-plan.md |
| 58 | Human CLI path + `control.sock` — `daedalus task create|dispatch|status|cancel|verify` as the deterministic reference client and security boundary; constrained, policy-checked ops only (never `run_shell`/`docker_run`/`mount`/`git_exec`/`start_container`, never `coordinator.sock`). CLI-first so the control plane is useful at N=1 and debuggable before any agent drives it; the Guild Master client is deferred to M15. Milestone 13 (V1). See docs/guild-master-plan.md |
| 59 | Job wrapper + isolated worktree + reconciliation — run each Job in a dedicated Git **worktree** checked out clean at `base_sha` (isolation as artifact-provenance, so the commit holds only the Job's changes, not a dev's dirty edits); Job ends at process exit; `execution_result` vs `output_snapshot` (only success → candidate; commit-exists ≠ succeeded); a reconcile-on-boot + periodic loop with idempotent, deterministically-named side-effects (the dual-write fix). Milestone 13 (V1). See docs/guild-master-plan.md |
| 60 | Independent verification — `candidate → verified` by checking out the Artifact's commit into a clean **verifier container** and running the project's `daedalus verify` policy (build + tests + `docs lint` + acceptance). Yields an *independently reproducible verification result*, not a proof of correctness; runner-agnostic; only the control plane can mark a Job verified. Milestone 14 (V1). See docs/guild-master-plan.md |
| 61 | Verify integrity — pin the verifier image by `sha256:` digest (not a mutable tag) + explicit network/creds/`/opt/tools` policy; **freeze + hash `acceptance_policy@base_sha`** AND a **test-integrity gate rejecting any Job whose diff touches the frozen test/acceptance files** (frontier agents game tests in 30–100% of adversarial runs). Ladder toward control-plane-supplied held-out tests. Milestone 14 (V1). See docs/guild-master-plan.md |
| 62 | Governance, integration & the Guild Master client — control-plane-enforced budgets + request rejection (over-budget; stale base); **integration as a race-safe transaction** (serialize → rebase → re-verify the *merged* result → compare-and-swap the ref — the merge-queue fix for semantic conflicts); a human `verified → approval_required → approved → integrated` state machine + Web/TUI; independent reviewer pass; event log. The **Guild Master joins as a gated client** (`guild-control-mcp`) with tiered, injection-safe authority (project docs untrusted; cancel/raise-budget/request-integration are human-confirmed proposals — breaks the lethal trifecta). Milestone 15 (V2). See docs/guild-master-plan.md |
| 63 | Parallel programme execution — multiple concurrent Jobs (worktrees already exist from M13, so this adds only concurrency + scheduling), a job scheduler with concurrency limits, and a cross-project task graph with dependency scheduling, composing with the `programmes` feature. Milestone 16 (V3). See docs/guild-master-plan.md |
| 64 | Typed steering (demoted) — `steer_job(job, instruction)` as a cancellable, audited `SteeringEvent` delivered at a supported boundary; plus cross-project task-board views over control-plane state. Low-priority: `cancel + redispatch` may suffice for short Jobs, so live steering should prove its value before earning a milestone. Milestone 17 (V3). See docs/guild-master-plan.md |
| 65 | **FIXED (Sprint 64)** — root cause was a one-word divergence: the writer's `milestoneHeadingRewriteRe` listed `Done\|In Progress\|Paused` while the parser's `milestoneHeaderRe` had gained `Planned`, so only a `(Planned)` heading doubled up. Stripping is now iterative (a corrupted heading repairs itself on the next write) and `ValidateWrite` refuses a doubled heading. Original report: `project-mgmt-mcp` status marker is appended, not replaced — `start_milestone` (and likely `pause_milestone`/`finish_milestone`) writes the new status onto a heading that already carries one, producing `### Milestone 15: … (V2) (Planned) (In Progress)`. Observed live opening M15 (2026-08-08); had to hand-repair the heading. The doc writer must strip a trailing `(Planned\|In Progress\|Paused\|Done)` marker before appending the new one. Note the parser is lenient enough that the double marker still parsed (status = the last one), so this is silent doc corruption, not a hard failure — add a `core.ValidateWrite` check that rejects a heading with two status markers |
| 66 | **FIXED (Sprint 64)** — `AddSprint` now replaces the Current Sprint section's body when it holds no sprint heading, leaves prose alone when one is present, and the docstring no longer claims items start `Pending`. Also closed the adjacent gap the second occurrence exposed: the writer emitted no `Goal:` line, so every opening needed a hand-edit. Original report: `add_sprint` leaves the between-milestones placeholder behind — opening Sprint 58 (2026-08-08) inserted the new sprint above the existing `_No active sprint, and no active milestone…_` paragraph instead of replacing it, so `## Current Sprint` held both a real sprint and a note saying there wasn't one. Had to hand-delete. The writer should drop any placeholder prose in the Current Sprint section when it adds a sprint. Related: the tool's own docstring says new items start `Pending`, but it (correctly) writes an **empty** status cell — `docs lint` rejects the literal word `Pending` as unrecognised. Fix the docstring, not the behaviour. Sibling of #65 |
| 67 | Flaky test: `internal/web` `TestWatchBranch_*` — **two distinct failure modes**, reproduced 3 failures across 16 loaded runs (2026-08-09, during Sprint 62, whose diff touches nothing under `internal/web`). (a) `PushesEmptyOnDetach` times out at ~3.1s — pure timing, the watcher polls git HEAD every 20ms and the assertion races a WebSocket push. (b) `PushesOnChange` fails in **10ms** with `Branch = "" want "feat/sidebar"` — **not timing**: the test helper writes HEAD with `os.WriteFile`, which truncates before writing, and the poller reads the file inside that truncated window. Production is unaffected — real git writes HEAD atomically via lock-file-plus-rename — so this is a test-infrastructure defect, not a concurrency bug. Fix BOTH: make the watcher's tick injectable and drive it deterministically (mode a), AND make the helper write atomically via temp-file + rename (mode b). Do not just raise the sleep; that hides mode (a) and does nothing for mode (b) |
| 68 | `RemoveDependency` for the cross-project task graph — a Task whose dependency **failed** is left `blocked` and `unsatisfiable` forever: `failed` is terminal (no outgoing edge in `legalTransitions`) and there is no way to drop a declared edge, so the only operator escape is to cancel the dependent and recreate it. That reaches the same outcome as the cancelled-dependency path, just by hand. Add `RemoveDependency` (plane-owned, agent-proposal-tier like declaring one), or a `failed → rejected` recovery edge mirroring Sprint 62's jobless-task recovery. Until then the docs correctly state that a failed dependency is permanent. Found in the Sprint 62 audit; the original justification for the failed/cancelled asymmetry ("a human may retry") was false. **Raised in priority by Sprint 64:** now that landing is gated on the graph, a wrong or obsolete edge blocks integration and not merely admission, so this is an operator escape hatch rather than a convenience |
| 69 | **Real execution termination** — make the wall-clock budget a kill instead of bookkeeping. Today `runUnderWallClock` races the runner against a deadline and settles the Job **row** on time, but a runner that ignores its context keeps running: `CoordinatorRunner` is `exec`-based and does not abort mid-flight, so the container can outlive the verdict and the plane cannot say the work stopped (documented honestly in `docs/control-plane.md`, corrected in Sprint 64 where the code claimed otherwise). Wants: a **persisted execution handle** per Job (container id / pid, durable so it survives a daemon restart), an **idempotent `Stop`/`Kill`** on the runner seam, and **capacity released only on CONFIRMED termination** — releasing a scheduler slot on an unconfirmed timeout is the same over-commit class as the crashed-Job slot leak Sprint 62 fixed. This is the prerequisite for the wall-clock axis ever being described as enforced end-to-end. Milestone-sized; deliberately out of Sprint 64 (control-plane correctness) because it is execution-substrate work. From `daedalus-development-branch-review.md` finding 4 |
| 70 | **A durable scheduler** — replace retry-driven admission with a real dispatch loop. Today `DispatchTask` is reachable **only** from the HTTP handler (`daemon.go:178`), the `waiting` map is **in-memory**, and the reconcile tick is the only thing that revisits work, so a daemon restart erases queue order and a Task admitted-but-not-dispatched depends on someone asking again. Wants: **persisted queue entries** in `control.db` (position/lease/reason durable across restart) plus a loop that dispatches when capacity frees — the fair-lease semantics of Sprint 61 preserved, but reconstructible after a crash rather than rebuilt by luck. Composes with #69: capacity frees on confirmed termination, and the loop is what consumes it. Milestone-sized; the review's fifth finding ("admission is not a scheduler") is self-documented at `scheduler.go:96-103` and this is the item that closes it |
| 71 | **Hostile black-box tests for the control plane** — the suite is thorough but **white-box**: it proves the implementation behaves as its author expects. The review's recommended scenarios, minus the three Sprint 64 already covered (a runner ignoring cancellation, a crash during supersede-and-replace, and a dependency added at every lifecycle state — see `steer_test.go` / `graph_test.go`): the **daemon killed between every pair of durable operations**; **concurrent HTTP/MCP clients** racing the same Task; **malformed or hand-edited `control.db`** (the negative-budget row scan is the only such backstop today); **restart during queue contention** (overlaps #70); **real Docker/container termination** (host-only — this environment has no daemon; overlaps #69); and **shared-repository integration with several dependent tasks** landing into one queue. Prefer driving these through the socket API rather than the service struct, since that is the boundary an agent actually reaches. Related, same review paragraph: move the miniature post-mortems some functions now carry into ADRs and keep comments on the invariant plus the immediate reason — the volume is a maintenance smell, and divergence is exactly what produced the Sprint 64 wall-clock correction |
| 72 | **FIXED (2026-08-16), container half pending a Docker host** — `core.GuildControlSocketMount` mounts `<data-dir>/.daedalus/control-agent.sock` at `/var/run/daedalus/control-agent.sock` for the Guild Master alone, fail-closed three ways (not the Guild Master → nil; not a real socket → nil, so a stopped plane cannot have Docker create a directory there; basename not exactly `control-agent.sock` → nil, which is what stops the HUMAN socket being mounted and silently granting full authority). `DAEDALUS_CONTROL_AGENT_SOCKET` is set only alongside a real mount so the host mount and the entrypoint gate cannot disagree, and `daedalus guild-master` starts the plane first because a bind-mount source must exist at `docker run` — non-fatal, a plane that will not start yields a warning and the read-only overseer. Verified without Docker by `scripts/verify-guild-control.sh static` (15/15), which drives the REAL sockets: an agent caller creates a task (allowed), has its cancel turned into a 422 proposal with the task still alive, is refused when it confirms its own proposal, and the same confirm on the human socket executes. Runbook + the four Docker-only checks: `docs/guild-control-verification.md`. Original report: **The Guild Master's control client is never wired — nothing mounts the restricted socket into its container.** `cmd/guild-control-mcp` ships (Dockerfile, build.sh, package-release.sh, both release workflows) and `entrypoint.sh` adds the `guild-control` MCP server **only when `control-agent.sock` is present**, which is right — that socket *is* the caller's authority. But the host side never provides it: `core.GuildMounts` returns only the read-only `/guild/<name>` doc mounts, `RunnerVolumeArgs` mounts no socket, and `DAEDALUS_CONTROL_AGENT_SOCKET` is never set for the container. So the `[ -S … ]` gate is always false, the tool is never registered, and the Guild Master remains M12's read-only overseer — the M15 "the Guild Master can finally act" path is **dormant in exactly the way `SteeringDeliverer` is**. Found while writing `docs/using-daedalus-control.md` (2026-08-15); the docs now say so rather than describing a workflow that cannot run. Fix: bind-mount `<data-dir>/.daedalus/control-agent.sock` into the Guild Master's container only (never `control.sock` — mounting the human socket would silently promote the agent to human authority, which is the one mistake this design cannot absorb), set `DAEDALUS_CONTROL_AGENT_SOCKET` alongside `DAEDALUS_GUILD_MASTER`, and ensure the daemon is running before that launch since the mount source must exist. Then an end-to-end host test: the Guild Master creates a Task (allowed), attempts a cancel (recorded as a proposal), and attempts to confirm its own proposal (refused). Host-only — this environment has no Docker |
| 73 | **Auto-rebuild fails when a Job dispatch triggers it — the Dockerfile is resolved relative to the wrong directory.** Observed on a real host (2026-08-08, in `control.log`): a Job launch hit the `NeedsRebuild` branch and died with `#1 transferring dockerfile: 2B done` then `ERROR: failed to solve: failed to read dockerfile: open Dockerfile: no such file or directory`, surfacing as `Error: auto-rebuilding image: exit status 1` and taking the Job with it. The build context is `cfg.ScriptDir` (`build.go:97`), which is `filepath.Dir(os.Executable())` — correct for a normal CLI invocation, evidently not for the `daedalus` the control plane spawns. Suspects, in order: the versioned install layout from M9 (`$PREFIX/versions/<v>/` vs the `current` symlink, so the exe dir may not be the dir holding Dockerfile/entrypoint.sh/docker-compose.yml), and symlink resolution in `os.Executable`. LATENT, not dormant: it only fires when the image is stale, so a Job dispatched after any runtime-file change hits it while every other Job passes. Fix: resolve the build context from the *runtime files' installed location* rather than from the executable's directory, and make the failure name which directory it looked in — the current message says only that a Dockerfile is missing, not from where. Add a regression test that a build spawned with a cwd and an exe dir that both lack a Dockerfile still finds one |
| 74 | **The hermetic verifier cannot run a language build for any non-vendored project — so the built-in policy is often the only runnable one.** `VerifierEnvPolicy` mounts nothing but the checkout and runs `--network none` with no caches, which is exactly right for an oracle the work cannot influence — and it means `go build ./...` / `npm test` / `mvn verify` fail on dependency resolution unless the project vendors its dependencies or its image already contains a warm cache. Daedalus's own repo is the case in point: it has no `vendor/`, so its only workable check today is the default `daedalus docs lint --ci`, which grades documents rather than code. Options to weigh, none free: (a) document vendoring as the price of verification (honest, shifts work to every project); (b) pre-warm a language cache INTO the project image, so the pinned digest carries its own dependencies (keeps the network off and stays reproducible — probably the best of these); (c) let `verify.json` declare a narrow, audited network allowance (weakens the guarantee — a check that can reach the network can be influenced by it); (d) mount a read-only host cache (re-introduces host state into the oracle, which Sprint 59 spent a milestone removing). Whatever is chosen, `docs/control-plane.md` should state the limit where it describes the verifier, because 'independently reproducible verification' currently implies a stronger check than most projects can actually run |
| 75 | **A long data-dir path kills the control plane, and the agent socket is the one that breaks first.** Unix `sun_path` is 108 bytes, and the two socket names differ by six characters — so there is a window where `control.sock` binds and `control-agent.sock` does not. Measured live (2026-08-17): a 107-character human path bound fine, the 113-character agent path failed with `listen unix …/control-agent.sock: bind: invalid argument`, and because `cmd/daedalus-control/main.go:196` treats that as `fatalf`, the WHOLE plane refuses to start — including the human path that would have worked. Hit with a deep scratch dir, not a normal install (`~/.local/share/daedalus/.cache/.daedalus/…` is ~55 characters), but a deep `$DAEDALUS_PREFIX`, a long username, or a container path can reach it, and the failure names neither the cause nor the limit. Fix, roughly in order of value: (a) validate both paths at startup against `len(path)+1 > 108` and fail with a message stating the limit and the actual length; (b) consider binding relative to a directory fd (chdir plus a short relative name) so the limit applies to the filename rather than the absolute path; (c) decide deliberately whether an agent-socket failure should be fatal — arguably a plane that can serve humans but not agents should start, log loudly, and let the Guild Master go read-only, which is exactly the degradation #72 already handles |
| 76 | **Held-out acceptance tests — the strong version of per-task checks.** `task create --check` (shipped 2026-08-17) closes the "did this task deliver what it promised" gap only as far as the check is something the agent cannot satisfy by writing it: the integrity gate protects files matching `acceptanceGlobs`, so a check naming a NEW test the Job itself authors is still self-grading. The strong form is a test the agent never sees — an acceptance file held HOST-SIDE (under the data dir, like `budgets.json`; never in the repo, never mounted into the Job worktree), copied into the VERIFIER checkout at verify time and run there. That is the "control-plane-supplied held-out tests" ladder M14's plan gestures at and nobody built. Questions worth settling before coding: where a held-out file lives and how it binds to a Task (a path on the Task row? a directory keyed by task id?); whether it is frozen by hash like the acceptance policy (it should be); how it composes with `acceptanceGlobs`, given a held-out file has no in-repo path to protect; and what the verifier reports when it fails, since the agent cannot be shown the test it failed — a rejection an operator can act on but an agent cannot reverse-engineer is the POINT, not a limitation. Related: #61 (test-integrity gate, done) and the still-stubbed reviewer rung |
| 77 | **PARTIALLY FIXED (2026-08-17) — a Job now has a log of its own; the transcript deletion and retention are still open.** `RealExecutor.RunWithEnvTee` copies the child's output to a caller-supplied writer as well as to the daemon's stdout, and `CoordinatorRunner.Run` points it at `JobLogPath(dataDir, jobID)` = `<DataDir>/.daedalus/jobs/<job-id>.log`, opened before the run at mode `0600` (raw agent output is exactly where a leaked token appears). The path is CHOSEN by the service, not the runner, so every runner writes where the plane will later look; `jobs.log_path` records it, additively migrated, and only once the file exists — a non-empty column is a promise there is something to read, not a guess. Existence rather than the outcome is the test because it is the one signal that survives a timeout (which abandons the runner mid-write), a cancellation, and an open failure alike. `task status` prints the path under the Job. stdout and stderr share ONE writer value deliberately: os/exec's `interfaceEqual` special case then hands the child a single pipe, which preserves its interleaving and removes the race two MultiWriters would have. `StubRunner` honours `LogPath` too, so the no-Docker smoke exercises the same chain. STILL OPEN, and the reason this is not closed: (b) the agent's session transcript in the throwaway job home is still deleted on every exit path including failure (`internal/control/runner.go` defer → `RemoveProject` → `cleanCache` → `os.RemoveAll`) — the log captures what the agent PRINTED, not what it thought, and failure is exactly when the transcript is wanted; (c) nothing prunes the logs, so they accumulate one file per Job forever, which is the churn the deregistration exists to prevent — the answer is probably a retention count, not "never delete". Original report: **A Job has no log of its own, and the one record worth having is deleted on failure.** Two mechanisms combine, and the second is the damaging one. (a) `RealExecutor.Run` wires the child's stdout/stderr straight to the daemon's (`internal/executor/executor.go:31-37`), and the daemon's go to the single shared `control.log` (`internal/control/bootstrap.go:39`) — so every Job's agent output is interleaved with every concurrent Job's and with the plane's own logging, keyed by nothing. (b) The agent's session transcript lives in the throwaway home `<DataDir>/daedalus-job-<id>/`, and the deferred deregistration deletes that home on EVERY exit path including failure (`internal/control/runner.go:115-119` → `RemoveProject` → `cleanCache` → `os.RemoveAll`, `internal/registry/registry.go:236` + `internal/registry/cache.go:33`). What survives in the DB is `RunOutcome.Detail` = `err.Error()`, i.e. "exit status 1". So the honest position today is that a Job which fails for any reason the agent itself reported leaves no readable account of why. Measured 2026-08-17: J-6's `Not logged in · authentication_failed` was recoverable ONLY because the `--force` bug (fixed in `f9532a4`) had aborted the cleanup and accidentally preserved the home — the diagnosis depended on a second defect, which is not a plan. Fix, in order of value: (a) tee the runner's output to `<DataDir>/.daedalus/jobs/<job-id>.log` and record the path on the Job row so `task events`/`task status` can point at it; (b) do not delete the job home on a FAILED job, or copy the transcript out before removing (the home is the agent's own account of the attempt, and failure is exactly when it is wanted); (c) consider whether a salvaged transcript should be pruned on some schedule, since keeping every failed Job's home forever is the churn the deregistration exists to prevent — the answer is probably a retention count, not "never delete". Related: #55 (mount coverage on the coordinator path) and the worktree-gitdir mount gap, which was diagnosed 2026-08-17 from `docker-compose.yml`, CONFIRMED by a real Job on 2026-08-19 (it reported `fatal: not a git repository: <host path>` and stopped), and FIXED the same day by mounting the repository read-only at `/gitcommon` with a shadowing `.git` pointer (`core/gitworktree.go`) |
| 78 | **FIXED (2026-08-18) — the built-in acceptance policy failed on advisory findings, so it rejected every Task in this repository regardless of the work done.** `DefaultAcceptancePolicy` ran `daedalus docs lint --ci`, and `--ci` treats a warning as a failure (`cmd/daedalus/docs.go:197`). A roadmap between milestones — a deliberate, supported state the linter itself exits 0 on — emits exactly one warning and zero errors. So the built-in oracle, which grades every project that has declared no `.daedalus/verify.json` (including daedalus), returned `verify_failed` on every artifact it was ever shown. Measured on a real host: T-8 ("restyle the Web UI sidebar") was rejected on `warning ROADMAP.md: no milestone is marked (In Progress)` having changed nothing outside `internal/web`; `0 error(s), 1 warning(s)`. Note what this cost — that verify was the FIRST one in the project's history to actually execute a check command (the entrypoint bypass, `c21b75a`, had only just landed), and the first real verdict the plane ever produced was about the repository it was handed rather than the work it was asked to judge. Fixed by dropping `--ci` from the default: the oracle now gates on what is broken, not on what is merely remarked upon, and a project that genuinely wants warnings fatal declares `--ci` in its own `verify.json` — one line, and an explicit choice rather than an inherited one. Pinned by `TestDefaultPolicyDoesNotGateOnAdvisoryFindings`, which DERIVES the requirement: it lints the real ROADMAP/SPRINTS, and only if they carry warnings and no errors does it assert the default check carries no warnings-fatal flag — so it stands down on its own if the repo's docs ever acquire a genuine error, which the default policy SHOULD reject. Verified by reverting (it fails with the exact `1 warning(s) and 0 error(s)` that rejected T-8). Also fixed alongside: `.gitignore` excluded `.daedalus/` as a DIRECTORY, and git does not descend into an excluded directory, so a committed verify policy was impossible and the negation needed to be `.daedalus/*` + `!.daedalus/verify.json` — a project could have declared a policy, seen it in the working tree, and had the plane silently freeze the default instead, since `ReadAcceptancePolicyAt` reads `git show <base_sha>:.daedalus/verify.json`. **STILL OPEN, and the more interesting half:** the default policy grades the repository's GLOBAL state, not the Task's delta. Even errors-only, a Task that touches one CSS file is answerable for a malformed ROADMAP it never opened — and the reverse, a project whose docs happen to be clean gets a green verdict that examined nothing about the change. The honest fix is for the built-in oracle to be delta-aware (or for the plane to distinguish "the artifact is bad" from "the base was already bad" by running the policy at base_sha too and only rejecting on findings the Job introduced). Related: #74 (the hermetic verifier cannot run a language build, which is WHY the docs linter is the only universally runnable check and why daedalus's own `verify.json` is thin), #76 (held-out acceptance tests) |
| 79 | **"Integrated" does not mean "on my branch", and nothing said so.** Reported by the operator immediately after the first successful integration (2026-08-18): T-8 landed, and the repository looked unchanged. It was working exactly as designed — integration advances the PLANE-OWNED target, authoritative in `control.db` and projected into the repo as `refs/daedalus/target`, which is deliberately not a branch anyone checks out precisely so landing can never disturb a working tree (`internal/control/target.go:52-61`). The design reason is sound and structural: keying the target by canonical repo path rather than a git ref is what closed the Sprint-59 oracle-laundering hole, because a linked worktree CAN write branch refs. But the consequence — that adopting the landed commit is always a manual `git merge --ff-only refs/daedalus/target` — appeared in no document and in no command output, so the first honest read of a successful landing was "nothing happened". PARTIALLY ADDRESSED 2026-08-18: `task integrate` now says in plain words that the branch was not changed and how to adopt the commit, `--into-branch` opts into a guarded fast-forward (refused on a detached HEAD, a dirty tree, or a diverged branch — and refusing never unlands anything, because the branch step sits outside the transaction), and `docs/using-daedalus-control.md` explains the ref. STILL OPEN, and the reason this is filed rather than closed: (a) the Web UI and TUI approval surfaces say "integrated" with no equivalent explanation, so the gap is only closed on the CLI; (b) there is no way to ask for the fast-forward AFTER the fact — an operator who lands without the flag has to know the git incantation, and `task target <project>` could reasonably offer to adopt; (c) a confirmed PROPOSAL never advances a branch (deliberate — the confirming human may not be in that checkout), which is right but means agent-initiated landings always leave the gap open. Related: #55, and the Sprint-59 design note in `docs/control-plane.md` |
| 80 | **FIXED 2026-08-21, after it killed a fifth Task.** `driveJob`'s default branch now sends the JOB to `failed` and the TASK to `rejected` with reason `execution_failed`, so the retry/replan ladder is reachable — the same asymmetry `reapJob` has used since Sprint 65, for the same reason: "did this attempt finish" and "is this work still worth doing" are different questions. The attempt is STILL CHARGED, because the plane cannot tell a broken environment from a genuinely bad run and refunding on an unsure reading would make a Job that fails instantly free to repeat forever. `ExecTimeout` routes the same way: a wall-clock overrun is a budget verdict about one attempt, and `retry` re-checks the budget anyway. **Measured five times before it was fixed: four Tasks in the `Not logged in` era, then T-15 — dead four seconds into a run with two of three attempts unspent, immediately after the operator had recreated it to replace a Task they had cancelled.** **The counter-argument the entry raised is now ANSWERED by what shipped rather than argued about: almost nothing reaches `failed` any more, and that is correct — `failed` is for "the plane cannot carry this out", and every execution outcome feeds the ladder.** Two tests updated to pin the new contract rather than the old one, and the dispatch test now asserts `retry` actually works from there, because the state name was never the point. Original report: **A runner-reported execution failure is still terminal, which is the same trap Sprint 65 fixed one case of.** `driveJob`'s default branch sends `ExecFailed`/`ExecTimeout` to `s.terminate(..., StateFailed, ...)` (`internal/control/service.go:902`), so an agent that exits non-zero drives its Task to a terminal state with no `dispatch`, `retry`, `replan` or `reverify` available — the objective and its remaining budget die with one bad attempt. The reconcile-reaped case was fixed 2026-08-18 (Job → `failed`, Task → `rejected`) precisely because a liveness reading can be wrong; this path has the same shape and a stronger argument, because an agent exiting 1 is a *legitimate attempt that did not work* and retrying is the textbook remedy. Measured: the `Not logged in` era (2026-08-17) produced four Tasks — J-2 through J-5 — each killed permanently by `runner exit code 1`, for an environment fault that had nothing to do with any of their objectives; every one had to be recreated by hand. The counter-argument to weigh before changing it: `failed` should mean something, and if both the reaped and the exec-failed paths route to `rejected` then almost nothing reaches `failed` any more — so the honest question is whether a non-terminal `failed` is wanted at all, or whether the state should be reserved for "the plane cannot carry this out" (a missing verifier, an unresolvable project) and execution outcomes should always feed the ladder. `ExecTimeout` deserves its own answer: a wall-clock overrun is a budget verdict, and `retry` re-checks the budget anyway. Related: the Sprint-65 `reapJob` comment, which states the reasoning this entry would generalise |
| 81 | **Long sessions exhaust the container's pid cap, because pid 1 does not reap — FIXED for the container (2026-08-19), but the leak upstream is worth knowing about.** `entrypoint.sh` execs `/usr/local/bin/daedalus-runner`, so the runner is pid 1; it calls `cmd.Wait()` on its one direct child and nothing else. Every process a tool orphans is re-parented to pid 1, exits, and stays a **zombie** — only a parent can clear one — and the pids cgroup counts each against `pids_limit: 512`. The container then cannot fork at all, and it is **unrecoverable from inside**: `kill -CHLD 1` does nothing, a zombie cannot be killed twice, and `pids.max` is on a read-only mount. Measured on a real session before the fix: **394 zombies of 405 processes, all with ppid=1 — 321 `chrome-headless`, 70 `esbuild`, 3 shell — against only 68 live tasks.** Fixed by `init: true` on the compose service: Docker's init (tini) reaps orphans in a loop, the runner becomes pid 2, and signals and exit codes still propagate (the control plane reads that exit code to decide `success → candidate`). **DELIBERATELY NOT done: a `SIGCHLD` handler inside daedalus-runner.** It is about twenty lines and it races with Go's `os/exec`, which reaps its own children — a blanket `waitpid(-1, WNOHANG)` can collect the agent's status before `cmd.Wait()` does, which then returns `waitid: no child processes`. The failure mode is not a crash but Jobs that succeeded being recorded as failures, intermittently, which is the one thing the runner exists to get right. Skipping the runner's own pid is possible but the skip is itself racy. **STILL OPEN, and the reason this is filed rather than closed:** (a) the numbers say the leak is not only browser probing — `esbuild` accounts for 70, which is every `npm run build`, `vitest run` and dev-server start, so ordinary development leaks too and only tini's presence now hides it; (b) `mem_limit`, `cpus` and `pids_limit` are hardcoded in `docker-compose.yml` with no config surface, so a project that genuinely needs more has to edit a shipped file; (c) existing containers keep their zombies — the fix only applies to containers created after it, and there is nothing that tells an operator to recreate one. Related: #27 (decoupling tooling from runner images) |
| 82 | **FIXED 2026-08-20 (same day).** `executeProposal` gained the three cases and `guild-control-mcp` gained `list_programmes`, `get_programme` and `propose_programme`; `scripts/verify-guild-control.sh static` is now 20/20 and drives the whole path over the REAL sockets — agent lists programmes (allowed), agent forms one (422 proposal_recorded), no programme appears, human confirms, the programme exists with its description intact. The proposal argument is JSON and deliberately NOT the split-on-first-separator trick `decodeSteerArgument` uses: that is sound there because a Job id cannot contain a space, and borrowing it here would borrow the style without the reason — a name or description with a colon would have been silently truncated. **The lesson is the half worth keeping: an operation was TIERED, documented in a milestone's deliverable list and marked Done while nothing could reach it.** Tiering reserves authority over something that has to exist. Original report: **The Guild Master cannot see or propose a programme, and M20's own description says it can.** M20 tiered three operations for it — `form_programme`, `amend_programme`, `dissolve_programme`, all `TierProposal` — and tiering is where the milestone stopped. Two things are missing and either alone makes the path dead. (1) **`cmd/guild-control-mcp` exposes no programme tool at all.** Its seven tools are `list_tasks`, `get_task`, `task_events`, `create_task`, `request_verification`, `programme_board` and `request_steering`; there is no `list_programmes`, so the Guild Master cannot even READ them — despite `agentAuthority` marking `OpProgrammes` TierAllowed with the comment that reading them is most of the point, since noticing what projects have in common is the whole job. Note `programme_board` is a red herring: it is the cross-project TASK board and has never had anything to do with programmes, which is the naming collision M20 existed to undo and did not finish undoing. (2) **`Service.resolveProposal`'s switch has no case for the three operations**, so a proposal that somehow got recorded would fail closed on confirmation with *"names an operation this plane cannot execute"* — correct behaviour for an unknown op, and a dead end for this one. Fix: add the three cases (form and amend need name + description through the single `Argument` column, so they need an encoding — `decodeSteerArgument` is the precedent for carrying two values through one column), and add `list_programmes` / `get_programme` / `propose_programme` to the MCP server. Worth doing because it is the ONE capability that makes the Guild Master's cross-project sight into something it can act on: today it can describe a common interest and has no way to propose that it become a programme. **The lesson is the more valuable half: an operation was tiered, documented in a milestone and marked Done while nothing could reach it — the prose-claims-more-than-the-code class, this time in a milestone's own deliverable list.** Related: M20, and #55's habit of one surface being wired while another is not |
| 83 | **DECIDED 2026-08-21: a Job cannot reach a git remote, and that is the design rather than a gap.** Found by the REVIEWER, not the plane: T-13's blocking finding was "the entire deliverable is a patch file plus a plan", and the Job's own log gave the cause — `~/.ssh` does not exist in the container, because `jobHomeSeedFiles` is a deliberate allow-list of Claude credentials and nothing else. **A fix was built and then REMOVED the same day**, and the removal is the entry worth keeping. It seeded SSH material per project, opt-in, from the host-side `budgets.json`. Two things were wrong with it. A container with the network, an untrusted objective and a push-capable key is a bad trade whatever gates it — ssh cannot scope a key to one repository, so the grant is always "write anywhere this key reaches, for the whole run". And the gating itself was the deeper error: it answered a capability question with a per-project configuration file, in a tool whose entire purpose is to remove administrative work. **A knob that must be maintained for every project is not a smaller cost than the missing feature; for this tool it is a larger one.** What shipped instead is one paragraph in `jobEnvironmentNote`: the Job is TOLD it has no remote, and told to stop and say so rather than produce a patch or a handoff document — which is not a smaller version of the work but a different deliverable, and which looks like a result until somebody reads it. Cost: one paragraph. Buys: a refusal in the first minute instead of a wasted attempt. **If this is ever revisited, the thing to reach for is a DEPLOY KEY scoped to one repository, or the plane pushing on the Job's behalf after verification — never a general credential in the container.** Related: the `Not logged in` era, the same defect for a different credential |
| 84 | **FIXED 2026-08-21 (same day).** `replan --rebase` re-pins the Task to the project's current target and re-freezes the acceptance policy there, then replaces the objective — one command for the correction that needed two doors and could not use either. It reuses the SAME `rebaseTaskToTip` that `retry --rebase` and `reverify --amended` already share; this is its THIRD caller, which is precisely when a copy starts to look reasonable and stops being so — one place re-freezes the oracle, so there is no third copy of the Sprint-59 laundering fix to forget. **Order matters and is deliberate: the re-pin happens FIRST, so a refused rebase leaves the objective alone** — correcting the instruction and then failing to move the base would leave a Task that reads as fixed and is not. Opt-in by name, like every other path that adopts a newer oracle. **A SIBLING GAP was found and fixed in the same pass: `replan` opened only from `rejected`, so an operator who realised the objective was wrong while the work sat UNGRADED had to run a verification they did not care about, purely to reach the state the ladder opens from — spending a review cycle to earn permission to say "I asked for the wrong thing", which is a toll rather than a safeguard.** New plane-only `candidate → planned` edge (a downgrade, absent from `workerReachable`, so it can launder nothing), and the ungraded Job is SET ASIDE as rejected rather than left live — a Job in `candidate` under a `planned` Task is an attempt nothing will ever move, and `candidateJob` would keep offering it to a verify about a different objective. **A test caught a real subtlety while being written: with no `.daedalus/verify.json` at either commit the default policy applies to both, so an unchanged acceptance hash is the CORRECT answer and an assertion on it proves nothing — the fixture now commits a policy at the new tip so the re-freeze has something to re-freeze to.** Original report: **A Task whose OBJECTIVE is wrong and whose BASE is stale has no path through the ladder.** The two remedies for a rejected Task are `replan` (new objective) and `retry --rebase` (re-pin to the current target, re-freeze the acceptance policy, dispatch), and each does exactly half of what is needed. `replanTask` (`service.go:1617`) changes the objective and moves the Task to `planned` — it does NOT touch `base_sha`, so the next dispatch runs against the tree the Task was created on. `prepareRetry` (`service.go:1522`) re-pins the base but carries the OLD objective, and both refuse from any state but `rejected` — so they cannot be chained: after a replan the Task is `planned`, which retry refuses. **The only route today is replan → dispatch → be rejected again by the stale base → THEN `retry --rebase`, spending an attempt to learn something already known.** Measured 2026-08-21 on T-13: its objective asked a Job to push (impossible — see #83) and its base predated T-14's lint fix, so both halves were wrong at once and the honest advice was "cancel it and create a new Task". That works and loses little — T-13's artifact was a patch file and a plan — but **abandon-and-recreate should not be the only door**, and it silently resets the record: a fresh Task carries none of the history, none of the reviews, and none of the reasoning about why the first attempt was wrong. Fix, in order of how small it is: (a) `replan --rebase`, reusing the SAME `rebaseTaskToTip` that retry and `reverify --amended` already share, so there is one place the acceptance policy is re-frozen and no third copy of the laundering fix to forget; (b) or `retry --rebase --objective <text>`, which is the same operation viewed from the other end. **(a) is preferable because replan already means "the instruction was wrong", and a stale base is a fact about WHEN it was wrong, not a different kind of correction.** Worth doing when the retry/replan ladder is next touched; it is a missing combination rather than a defect in either half. Related: M19's `reverify --amended`, which had the same shape of problem (a correction the ladder could not express) and got its own door |
| 85 | **FIXED 2026-08-21 (M21, Sprint 68).** `guild-control-mcp` gained `propose_programme_amendment` and `propose_programme_dissolution`, so the Guild Master can now describe a programme's whole life rather than only its beginning. Both operations had been tiered since programmes landed and both gained an `executeProposal` case in #82 — only the agent-facing tools were missing, which is #82's own shape one notch smaller. The amendment MERGES rather than replaces: an omitted field is left as it was, because the agent is describing a change and a tool that dropped the project list while fixing a description would be a data-loss bug wearing the shape of an update. The merge happens at proposal time, so the human confirming sees the finished programme in the argument instead of a patch to apply in their head. **Dissolution deliberately carries no `why`**: `callerScope` builds its argument as `dissolve <id>` with no room for one, and a reason the record silently dropped would be worse than an absent field — the agent would believe it had explained itself to whoever has to decide. Carrying one would mean widening `DeleteProgramme` through `TaskAPI`, which is a bigger change than the value justifies and is written down here rather than done quietly. Four new assertions in `scripts/verify-guild-control.sh` drive both over the REAL sockets: agent amends → 422 proposal_recorded, nothing changes, human confirms, the programme reads as amended; then the same for dissolution. Original report: **the Guild Master could propose a programme into existence and then never say another word about it.** Noticing that a programme has drifted from what it was formed for is the same act as noticing it should exist, and only one of the two was reachable. Related: #82, and the tiering lesson it records |
| 86 | **FIXED 2026-08-21 (M21, Sprint 68).** The MCP tool `programme_board` is now `task_board`, and `OpBoard` with it. The board groups TASKS by whose move it is and has never had anything to do with programmes; the collision predates programmes being plane state, and it cost #82 an entire paragraph explaining that the tool is a red herring — which is the definition of a name that is doing damage. The old tool name is REGISTERED TO THE SAME HANDLER and marked deprecated rather than deleted, because a Guild Master's `CLAUDE.md` may name it and a rename that breaks a running agent's instructions is a rename that gets reverted. The operation constant was safe to rename because it is `TierAllowed` and a read is never recorded as a proposal, so no stored row carries the old string — stated in the code beside the change, since "safe to rename" is exactly the kind of claim that should carry its reason. Related: M20, which set out to undo this collision and undid it in the data model only |
| 87 | **FIXED 2026-08-21 (M21, Sprint 68).** `docs/m20-verification.md` claimed, in its own known-gaps list, that *"you can see a Task's programme in its entry"* in the Ledger. You could not: `internal/web/static/control.js` contained no reference to `programme` and none to `rationale`, and the approvals payload the page polls carried neither field. What was true is that the CLI printed both and the REST routes served them, which is a different claim. **The entry is corrected and left recorded rather than edited away**, exactly as the #82 entry beside it is, because that document exists to catch prose claiming more than the code and it did it about itself. The surface it described was then built in M21. **The general lesson is the one this repository keeps re-learning in new places: a document that describes a capability is not evidence the capability exists, and a known-gaps list is not exempt from being wrong in the optimistic direction.** Related: #82, #55 |
| 88 | **FIXED 2026-08-22 (same day it was found).** `create_task` — the agent-facing MCP tool the Guild Master files work through — now takes an optional `programme` (an id like `PR-3`, or a name) and an optional `rationale`, and both reach the plane. **Until this, every task the Guild Master filed was an orphan**: the seat whose entire job is noticing what projects have in common could list programmes, propose one, amend and dissolve one — and could not attach a single task to any of them. The plane has carried `CreateTaskRequest.Programme` and `Rationale` since Sprint 66 and a human's CLI has passed them since; only the agent's own tool dropped both on the floor. **That is the third instance of one shape in three days — #82 (no programme tools at all), #85 (form but not amend/dissolve), and now this — and the shape is: the plane supports an operation, a human can reach it, and the agent-facing surface was never widened to match.** The reference is passed through UNRESOLVED and the plane matches it against `control.db` by id or name, so a Task can only ever point at a programme that exists; a reference that resolves to nothing REFUSES the create rather than filing a task that reads as attached to whoever wrote the request and belongs to none in the data. `rationale_by` is still derived from the socket, so an agent's reason is stored visibly as the agent's. The request-building is a pure `createTaskRequest` with three tests (the fields carry, both are optional, an all-zero budget is not attached — the plane reads that as a request for zero attempts). `scripts/verify-guild-control.sh static` is **35/35** and drives the whole path over the real sockets: the agent creates a task naming a programme and the response carries `PR-n` and `rationaleBy: agent`, and an unknown programme comes back 404 with no task filed. The Guild Master's role doc gained the instruction to name a programme, which made this the first real exercise of the role-doc refresh built the day before — an installed M21 doc now classifies as outdated and is brought forward on the next launch. **Found by T-17**, a Task the plane itself dispatched, whose diagnosis was right and whose implementation could not be used: it was built on a base four days stale (`c8d761f`, before Sprint 66), so it invented a file-backed programme model with the name as identity — precisely what M20 removed. See #89 for the reason it had a stale base at all |
| 89 | **FIXED 2026-08-22 (same day it was found).** The gap is now reported at the two moments it matters, and refuses nothing. `daedalus task create` WARNS when the integration target trails the checkout — that is the moment somebody decides to spend an agent, and the base a Task freezes is the target rather than the branch they are looking at — and `daedalus control status` reports it per project, which is where an operator already looks to ask whether the plane is in a good state. `Service.TargetLagFor`/`TargetLags` compute it once, on `TaskAPI`, so the CLI, the daemon and the Ledger cannot describe the same gap differently; `TargetLag.Summary()` is the single rendering. **Divergence is reported separately from a count**: a target that is no longer an ancestor of HEAD (a rebase or a reset moved the branch) gets its own sentence, because "behind by 4" and "not on this history any more" need different answers and `rev-list` across a divergence returns a number that means nothing. A comparison that cannot be made answers `unknown` rather than staying silent — silence reads as "up to date", the one answer this must never give wrongly — and a project with no target yet is NOT reported, because there is genuinely nothing to be behind and a warning that fires when nothing is wrong is one nobody reads. **Nothing refuses**: a branch deliberately ahead of the target is a normal way to work, and the operator is the one who knows whether the gap is intended. The derived web-surface test forced the Ledger route too, which is right — this is precisely the failure nobody can deduce from a screen. Exercised end to end: a fresh target is quiet, four unsynced commits produce the warning at create AND in `control status`, and a sync silences both. Four unit tests. **Deliberately NOT done: making the target follow the checkout automatically.** The target being plane-owned, and not a git ref anybody can move, is the Sprint-59 fix that closed oracle laundering; having it track a branch would hand that back. Original report: **the integration target silently fell four days behind the branch, and the plane had no way to notice.** Found when T-17 landed. `refs/daedalus/target` only moves when work integrates or somebody runs `task target --sync`. Seven commits were pushed to `development` that day and it was never synced, so it sat at `c8d761f` (2026-08-18) — before Sprint 66 — and the plane dispatched T-17 against a tree in which plane-owned programmes did not exist. The agent's work was coherent for the tree it was given and obsolete for the repository, and reviewing it cost more than the feature. **`stale_base` cannot catch this**: it compares a candidate's base against the *target tip*, and the target tip WAS its base. The plane was perfectly consistent with itself and four days behind the world. Related: #79 (integrated does not mean on your branch), #88 (what T-17 found), and the Sprint-59 target design in `docs/control-plane.md` |
